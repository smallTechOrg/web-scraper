DEBUG = True
GROQ_API_KEY = your_groq_api_key_here
GROQ_MODEL_NAME = meta-llama/llama-4-scout-17b-16e-instruct

# Comma-separated list of base URLs allowed via the X-Backend-Url header (SSRF allowlist).
# Every caller (including curl tests) must send a valid X-Backend-Url header matching one of these.
ALLOWED_BACKEND_URLS = https://staging.example.com,https://prod.example.com